# SME Post-check — Python dependencies
# Install: pip install -r requirements.txt
# Required outside pip:
#   - Tesseract OCR (only when a dossier holds a scanned PDF)
#   - WeasyPrint native libs (Pango/Cairo/GLib), only when exporting the PDF:
#     macOS:  brew install pango cairo glib
#     Debian: apt-get install libpango-1.0-0 libcairo2 libgdk-pixbuf2.0-0

# --- LLM: four extraction passes and two commentary paragraphs ---
# langgraph: the post-check flow is a linear 8-node graph. There is no routing to
# speak of, so the value is not in the branching but in the step order being
# DECLARED in one place instead of implied by statement order inside a function.
langgraph==1.2.6
langchain==1.3.11
langchain-core==1.4.8
langchain-openai==1.3.2
openai==2.43.0

# --- Document reading & OCR ---
pypdfium2==5.12.1
pytesseract==0.3.13
opencv-python==4.13.0.92
numpy==2.4.6
pillow==12.2.0
pandas==2.3.3
openpyxl==3.1.5          # reads .xlsx
xlrd==2.0.2              # reads legacy .xls
python-docx==1.2.0       # reads .docx - BRD 2.3.a allows the Word format
markdown==3.10.3         # Markdown -> HTML for the PDF export
weasyprint==69.0         # HTML -> PDF

# --- Configuration ---
pyyaml==6.0.3            # config/programs.yaml, document_matrix.yaml

# --- Runtime / notebook ---
python-dotenv==1.2.2
ipython==9.14.1
# Strips notebook output on commit, so real customer data never reaches the repo.
# Run `nbstripout --install` once per clone.
nbstripout==0.9.1
